#!/bin/bash

echo "Список .conf файлов в директории /etc (регистронезависимый поиск):"
echo "--------------------------------------------------------------"

ls -l /etc | grep -i "\.conf$"


if [ $? -ne 0 ]; then
    echo "Файлы с расширением .conf не найдены в /etc"
fi
